# Монетки

PWA личного учёта финансов (замена CoinKeeper). Быстрый ввод трат: тап по монетке
категории → цифровая клавиатура → готово. Работает офлайн, данные принадлежат пользователю.

**Статус: v1.0 — первая рабочая версия.**

## Возможности (v1.0)

- Дашборд: сводный баланс (приведён к гривне по актуальному курсу Monobank), расходы месяца, план.
- Монетки категорий с лимитами и цветовой логикой CoinKeeper.
- Ввод: расход, доход, перевод между счетами, правка баланса.
- Правка и удаление операции (тап по строке в «Последних операциях»), балансы пересчитываются.
- Создание/редактирование категорий, счетов, источников доходов.
- Источник истины — **IndexedDB** на устройстве; работает без интернета.
- Устанавливается на домашний экран (PWA), светлая/тёмная темы, русский интерфейс.
- **Экспорт в vault** (меню «Экспорт в vault»): снапшот в Markdown с Dataview-инлайн-полями;
  на ПК — прямая запись в файл, на телефоне — сохранение/«Загрузки» под синхронизацию FolderSync.

## Приватность

Реальные балансы и операции хранятся **только на устройстве (IndexedDB)** и в vault Obsidian.
В репозитории — лишь оболочка приложения и структура справочников; балансы в `js/seed.js`
обнулены. Секрет прослойки синхронизации в репозиторий не попадает (см. `config.local.js`,
он в `.gitignore`).

## Структура

```
index.html              оболочка
styles.css              стили
js/seed.js              стартовые справочники (балансы = 0)
js/db.js                IndexedDB — источник истины
js/rates.js             курсы Monobank (кэш + офлайн-фолбэк)
js/vault.js             экспорт в vault (Markdown §4, File System Access / download)
js/app.js               логика: рендер, ввод, редакторы
manifest.webmanifest    PWA-манифест
service-worker.js       офлайн-оболочка
icons/                  иконки 192/512
```

## Локальный запуск

PWA требует http(s) (Service Worker + IndexedDB не работают с `file://`). Любой статический
сервер, например:

```bash
npx serve .
```

и открыть выданный `http://localhost:...`.

## Деплой на GitHub Pages

Settings → Pages → Source: ветка `main`, папка `/` (root). Через минуту приложение доступно
по адресу `https://<username>.github.io/<repo>/` — оттуда ставится на телефон.

## Синхронизация в vault

Путь **без сервера**: «Экспорт в vault» пишет Markdown (Dataview-инлайн-поля), а перенос в
Google Drive/vault делает **FolderSync** на телефоне. IndexedDB остаётся источником истины —
приложение экспортный файл не читает, поэтому его размер на приложение не влияет.

Два режима (выбираются по устройству):
- **Папка, помесячно** (ПК/устройства с доступом к папке): `Финансы — ГГГГ-ММ.md` по месяцам
  + `_Счета.md` + `_Категории.md`. Файлы остаются лёгкими для Obsidian/Dataview.
- **Один файл** (телефон без доступа к папке): снапшот в один файл с перезаписью.

## Дальше (после v1.0)

- Помесячные файлы `_Данные/ГГГГ-ММ.md` вместо одного снапшота (десктопный сценарий записи в папку).
- Опционально — авто-синхронизация через прослойку на Google Apps Script (фон, без ручного экспорта).
- Правка/удаление отдельной операции, навигация по месяцам, доводка иконок/сплэша.
